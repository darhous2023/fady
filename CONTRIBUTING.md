# Contributing

- Preserve the existing architecture unless evidence requires a change.
- Document architecture changes in `docs/ARCHITECTURE.md` and `docs/DECISION_REGISTER.md`.
- Add database changes through Drizzle schema and migrations.
- Never commit secrets.
- Run validation before commit.
